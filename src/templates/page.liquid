<div class="page page--default animated-pages">
    <div class="container">
        <div class="page__header ">
            {% include 'breadcrumbs' %}
        </div>
        <div class="page__title ">
            <h1>{{ page.title }}</h1>
        </div>
        <div class="page__content ">{{ page.content }}</div>
    </div>
</div>
